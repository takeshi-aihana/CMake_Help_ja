.. note::

  ``${VAR}`` 形式で変数の参照や評価をする場合、CMake は最初にその名前を持つ通常の変数を探す。
  通常の変数が見つからなかったら、次はその名前を持つキャッシュ変数を探す。
  そのため、**通常の変数の値を解除すると、それまで隠されていたキャッシュ変数が公開される可能性がある**。
  ``${VAR}`` 形式で変数を参照した際に、強制的に空の文字列を返すようにするには ``set(<variable> "")`` を使用する。
  これにより通常の変数はクリアされますが、値は定義されたままになる。
